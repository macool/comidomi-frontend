.button.cart-main-button(
  ui-sref='app.cart.index'
)
  span.badge.badge-light.number-of-items(
    ng-if="cartButtonVM.numberItems > 0"
  ) {{ cartButtonVM.numberItems }}
  ng-md-icon.icon(
    icon="shopping_cart"
    size="30"
  )
